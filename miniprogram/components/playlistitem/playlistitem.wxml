<view class="playlist-container" bindtap="goToMusiclist">
  <image src="{{playlist.picUrl}}" lazy-load class="playlist-img"></image>
  <text class="playlist-playcount">{{_count}}</text>
  <view class="playlist-name">{{playlist.name}}</view>
</view>